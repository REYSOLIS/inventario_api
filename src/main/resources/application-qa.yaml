spring:
  application:
    name: productos-api

  datasource:
    url: jdbc:mariadb://localhost:3306/catalogo_productos
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret-key: mi-clave-super-secreta-para-el-examen
  expiration: 86400000
